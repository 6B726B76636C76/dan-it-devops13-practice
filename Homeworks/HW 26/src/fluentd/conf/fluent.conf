<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match winston.**>
  @type elasticsearch
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix js-app-logs
  include_tag_key true
  tag_key @log_name
  flush_interval 5s
</match>